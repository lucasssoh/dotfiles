# Wallpapers listed here (by filename glob pattern, one `*` wildcard
# supported per line) use "safe" mode in wallpaper-filter.rs: the axis
# carrying the main subject is never cropped, and a too-short free axis is
# extended with a blurred background instead. Meant for portraits,
# character art, or anything else where the whole subject must stay
# visible.
#
# Everything NOT matched here uses the default "fill" mode (plain cover +
# center-crop, no blur) -- abstract/pattern art and landscape photos,
# where cropping doesn't lose anything worth protecting.
#
# Lines starting with # and blank lines are ignored.

oleksandr-kozachenko-hands-study-5.jpg
