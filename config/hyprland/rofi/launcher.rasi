/* rofi/launcher.rasi — barre de recherche (drun / launcher, powermenu…).
 * Couleurs et style de sélection hérités de share.rasi ; structure seule ici. */
@import "share"

window {
    width: 520px;
}

inputbar {
    margin: 0px 0px 10px 0px;
}

prompt {
    margin: 0px 8px 0px 0px;
}

entry {
    placeholder: "Search...";
}

listview {
    lines:         8;
    fixed-columns: true;
}

element-icon {
    size:           24px;
    vertical-align: 0.5;
}

element urgent {
    text-color: @urgent;
}

element normal.urgent {
    text-color: @urgent;
}
